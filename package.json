{
  "name": "api-project",
  "version": "1.0.0",
  "description": "api",
  "main": "index.js",

  "scripts": {
    "dev:server": "nodemon --watch server",
    "start": "run-s build server",
    "build": "webpack && knex migrate:latest",
    "server": "node server",
    "test": "jest --watchAll"
  },
  "author": "Jefferson Matamba",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.932.0",
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.8"
  }
}
